import React from 'react';
import Tr from './tr';
import PropTypes from 'prop-types';

const Tbody = (props) => {
    return (
        <Tr/>
    );
};

Tbody.propTypes = {
    picArray: PropTypes.array,
};

export default Tbody;