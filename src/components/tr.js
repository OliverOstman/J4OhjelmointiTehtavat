import React from 'react';
import PropTypes from 'prop-types';

const Tr = (props) => {
    return (
        <h1>Tr</h1>
    );
};

Tr.propTypes = {
    picArray: PropTypes.array,
};

export default Tr;